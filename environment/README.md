# Verified environment

The frozen Phase 2C neural experiment ran locally on CPU with Python 3.11.9 and PyTorch 2.13.0+cpu. The complete directly observed environment is recorded in `runtime_environment.csv`. The root `requirements.txt` and `requirements-lock.txt` reproduce the exact installed Python package versions captured for that run; they are not claims about other operating systems or future package availability.

Release staging was prepared on Windows-10-10.0.26200-SP0 at source commit `1a872a414dbeb200bce0960af2d905b1db5ba7ed`. No container image or cross-platform equivalence claim is made.
